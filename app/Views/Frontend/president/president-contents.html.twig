{% extends FRONTEND_TEMPLATE_PATH ~ '/layouts/default.html.twig' %}

{% import FRONTEND_TEMPLATE_PATH ~ '/' ~ folder ~ '/partials/menu.html.twig' as macro %}

{% block body %}
  <!-- Content Header Start -->
  <section class="py-6 2xl:py-12 bg-primary-900 relative overflow-hidden">
    <!-- Background Layer Start -->
    <div class="absolute top-0 left-0 size-full z-[10] mix-blend-overlay select-none">
      <img class="absolute size-full object-cover opacity-20 blur-lg" src="{{ webp_url(FILE_PATH_ASSETS ~ '/' ~ FILE_PATH_IMAGES ~ '/general/section-bg.webp') }}" />
    </div>
    <!-- Background Layer End -->

    <!-- Background Overlay Layer Start -->
    <div class="hidden absolute z-[15] size-[85%] top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 rounded-full bg-white/10"></div>
    <!-- Background Overlay Layer End -->

    <div class="relative z-[30]">
      <div class="container px-6 mx-auto">
        <div class="text-2xl md:text-3xl lg:text-3xl font-bold text-white mb-0.5">{{ result.president_content_name }}</div>
        <div class="flex flex-nowrap items-center overflow-x-auto overflow-y-hidden gap-1.5">
          <a href="#" class="text-sm text-white opacity-75 font-light transition-all hover:opacity-100 duration-300">{{ lang('Web.general.homePage')|upper }}</a>
          <i class="fal fa-angle-right text-white text-xs opacity-75"></i>
          <a href="#" class="text-sm text-white opacity-75 font-light transition-all hover:opacity-100 duration-300">{{ lang('WebPresident.title')|upper }}</a>
          <i class="fal fa-angle-right text-white text-xs opacity-75"></i>
          <span class="text-sm text-white font-normal">{{ lang('WebPresident.title')|upper }}</span>
        </div>
      </div>
    </div>
  </section>
  <!-- Content Header End -->

  <!-- Content Start -->
  <section class="py-6">
    <div class="container mx-auto px-6 flex flex-col lg:flex-row lg:gap-8">
      {{ macro.menu(list.left_menu) }}
      <div class="flex flex-col gap-y-6 gap-x-10 2xl:flex-row 2xl:items-start">
        <!-- Select on Mobile Start -->
        {{ macro.mobileMenu(list.left_menu) }}
        <!-- Select on Mobile End -->

        <img class="w-full rounded-xl lg:w-[400px] 2xl:order-2 2xl:shrink-0 2xl:w-[470px] 2xl:h-[470px]" src="{{ result.president.informations.president_image.base }}" />

        <div class="html-content 2xl:order-1">{{ result.president_content_description|raw }}</div>
      </div>
    </div>
  </section>
  <!-- Content End -->
{% endblock %}
